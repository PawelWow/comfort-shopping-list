export const STORAGE_KEY_CURRENT_LIST = 'CurrentList';

// local storage model for current list settings
class CurrentListSettings{
    constructor(id){
        this.id = id;
    }
}

export default CurrentListSettings;