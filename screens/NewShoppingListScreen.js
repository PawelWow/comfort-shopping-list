import React from 'react';
import {View, Text, StyleSheet } from 'react-native';

const NewShoppingListScreen = () => {
    return (
        <View><Text>Create new list</Text></View>
    );
};

const styles = StyleSheet.create({});

export default NewShoppingListScreen;